import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class ReservationService {
  //this file is for api interaction
  //this is only for file structure if incase this project needs api interactions
}
